// Beer API
export const API_URL = 'https://api.punkapi.com/v2/beers';

// Default image
export const DEFAULT_IMAGE_URL = 'https://images.punkapi.com/v2/keg.png';

// Max size of upload image
export const IMAGE_UPLOAD_MAX_SIZE = 1000000;

// Error message if image uploaded max size is over max
export const IMAGE_UPLOAD_MAX_SIZE_MESSAGE =
  'Image size exceeds the limit of 1MB';
